name: sijemur
description: A new Flutter project.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  get: ^4.6.5  # Menambahkan GetX
  google_maps_flutter: ^2.2.0  # Menambahkan Google Maps Flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/pngtree-blue-washing-machine-for-laundry-logo-png-image_8517358.png
